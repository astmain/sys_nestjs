import { filter_cors } from './filter_cors'
import { filter_dto } from './filter_dto'
import { swagger_knife4j2 } from './swagger_knife4j2'

// 配置:函数
export const Plugins = {
  filter_cors,
  filter_dto,
  swagger_knife4j2,
}
